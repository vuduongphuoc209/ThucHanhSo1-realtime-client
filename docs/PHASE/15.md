PHASE 19 — Kết nối React với Chat API
Bây giờ frontend cần API service.
client/src/services/chatApi.ts
Cập nhật Chat UI ==> Thay Chat.tsx bằng phiên bản có API thật:

PHASE 20 — Socket.IO
Kiến trúc:
┌─────────────────┐
│ MongoDB Atlas │
└────────▲────────┘
│
persist data
│
┌────────┴────────┐
│ Node + Express │
│ Socket.IO │
└───────┬─────────┘
│
┌────────────┴────────────┐
│ │
WebSocket WebSocket
│ │
┌──────▼──────┐ ┌──────▼──────┐
│ React A │ │ React B │
│ User A │ │ User B │
└─────────────┘ └─────────────┘

Khi A gửi tin:
User A
│
│ send_message
▼
Socket.IO Server
│
├── Validate JWT
├── Check conversation
├── Save Message → MongoDB
│
└── new_message
│
▼
User B

20.1. Cài Socket.IO
20.2. Tạo Socket Authentication
20.3. Tạo Chat Events
20.4. Presence — Online / Offline
20.5. Socket Server
20.6. Gắn Socket.IO vào Server
