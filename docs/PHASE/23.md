30. Test Offline

Mở app:
http://localhost:4173
Sau đó:
DevTools
↓
Network
↓
Offline

Refresh.

Nếu cấu hình đúng, app vẫn mở được

Internet
↓
❌
React application
↓
Service Worker
↓
Cache
↓
React UI

Lưu ý: khi test service worker, nên dùng production build + preview; tài liệu Vite PWA cũng lưu ý việc kiểm tra offline trong DevTools và tránh hard refresh khi đang offline
